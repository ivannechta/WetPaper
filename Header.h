#pragma once
typedef unsigned int uint;
#define GF_2 true
#define maxAttemptRank 100

#define EPS 0.00001
int min(int a, int b) { return (a < b) ? a : b; }
int max(int a, int b) { return (a > b) ? a : b; }

